<Window x:Class="WPFPrintingService.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFPrintingService"
        mc:Ignorable="d"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="Printing Service" Height="600
        " Width="1500" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" Closing="Window_Closing">
    <Grid x:Name="mainGrid">
        <Button x:Name="btnStartStopServer" Content="Start" HorizontalAlignment="Left" Margin="24,33,0,0" VerticalAlignment="Top" Height="33" Width="100" Click="btnStartStopServer_Click"/>
        <!--<ListView x:Name="lvConnectWebSocketClients" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="24,126,553,26">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="IP" DisplayMemberBinding="{Binding Ip}"/>
                </GridView>
            </ListView.View>
        </ListView>-->


        <DataGrid x:Name="lvConnectWebSocketClients" Margin="24,186,1178,128" SelectionMode="Single" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="IP" Binding="{Binding Ip}" Width="*"/>
                <DataGridTemplateColumn Header="Actions">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Monitor"/>
                                <Button Content="Print Test"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>




        <Label Content="Connect Clients" HorizontalAlignment="Left" Margin="24,160,0,0" VerticalAlignment="Top"/>
        <Button x:Name="btnAddPrinter" Content="Manual Add Printer" HorizontalAlignment="Left" Margin="564,34,0,0" VerticalAlignment="Top" Height="32" Width="133" Click="btnAddPrinter_Click"/>

        <DataGrid x:Name="dgConnectedPrinters" CanUserSortColumns="False" CanUserResizeRows="false"  Margin="435,105,803,138" SelectionMode="Single" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" RenderTransformOrigin="1,1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding PrinterName}"/>
                <DataGridTextColumn Header="IP" Binding="{Binding PrinterIp}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IsOnline}" Width="*"/>
                <DataGridTemplateColumn Header="Actions">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Remove" Click="Remove_Printer_Button_Click" />
                                <Button Content="Print Test" Click="Print_Test_Button_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnMonitorWebSocketServer" Content="Monitor" HorizontalAlignment="Left" Margin="148,34,0,0" VerticalAlignment="Top" Height="32" Width="99" Click="btnMonitorWebSocketServer_Click"/>
        <TextBox x:Name="txtServerStatus" HorizontalAlignment="Left" Margin="24,105,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="223" Height="50"/>
        <Button x:Name="btnServerInfo" Content="Info" HorizontalAlignment="Left" Margin="283,33,0,0" VerticalAlignment="Top" Height="32" Width="99" Click="btnServerInfo_Click"/>


        <tb:TaskbarIcon x:Name="myNotifyIcon"
                    Visibility="Hidden"
                    ToolTipText="Printing Servcie"
                    IconSource="/Icons/cherry.ico"
                    PopupActivation="RightClick"
                    TrayMouseDoubleClick="myNotifyIcon_TrayMouseDoubleClick"
                        >
            <tb:TaskbarIcon.TrayToolTip>
                <Border
                    Background="White"
                    BorderBrush="Orange"
                    BorderThickness="2"
                    CornerRadius="4"
                    Opacity="0.8"
                    Width="160"
                    Height="40"
                >
                    <TextBlock
                    Text="hello world"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                />
                </Border>
            </tb:TaskbarIcon.TrayToolTip>


            <tb:TaskbarIcon.TrayPopup>
                <Border
                  Background="White"
                  BorderBrush="Orange"
                  BorderThickness="2"
                  CornerRadius="4"
                  Width="160"
                  Height="40">
                    <Button x:Name="btnExitPrintingServiceViaSystemTray"
                    Content="Exit"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" Click="btnExitPrintingServiceViaSystemTray_Click" />
                </Border>
            </tb:TaskbarIcon.TrayPopup>


            <!-- Set a simple context menu  -->
            <!--<tb:TaskbarIcon.ContextMenu>
                <ContextMenu Background="LightCoral">
                    <MenuItem Header="First Menu Item" />
                    <MenuItem Header="Second Menu Item"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>-->

        </tb:TaskbarIcon>
        <CheckBox x:Name="chbAutoRunService" Content="Auto Run Service" HorizontalAlignment="Left" Margin="24,560,0,0" VerticalAlignment="Top" Width="129"/>
        <CheckBox x:Name="chbRunOnStartUp" Content="Run on Startup" HorizontalAlignment="Left" Margin="669,548,0,0" VerticalAlignment="Top" Checked="chbRunOnStartUp_Checked" Unchecked="chbRunOnStartUp_Unchecked"/>
        <Button x:Name="btnQuitApplication" Content="Quit" HorizontalAlignment="Left" Margin="564,546,0,0" VerticalAlignment="Top" Width="78" Click="btnQuitApplication_Click"/>
        <Button x:Name="btnSearchPrinter" Content="Search Printer" HorizontalAlignment="Left" Margin="435,31,0,0" VerticalAlignment="Top" Height="36" Width="97"/>
        <TextBox x:Name="txtMessage" HorizontalAlignment="Left" Margin="24,483,0,0" TextWrapping="Wrap" Text="Hello Everyone From Server" VerticalAlignment="Top" Width="147" Height="54"/>
        <Button x:Name="btnSendToEveryClients" Content="Send To Every Clients" HorizontalAlignment="Left" Margin="178,483,0,0" VerticalAlignment="Top" Height="54" Width="144" Click="btnSendToEveryClients_Click"/>
        <DataGrid x:Name="dgPrinters" CanUserSortColumns="False" CanUserResizeRows="false"  Margin="729,105,23,138" SelectionMode="Single" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" RenderTransformOrigin="1,1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding PrinterName}"/>
                <DataGridTemplateColumn Header="Actions" Width="*">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnPrintAndCut" Content="Print And Cut" Click="btnPrintAndCut_Click"/>
                                <Button x:Name="btnPrintAndKickDrawer" Content="Print And Kich Cash Drawer"/>
                                <Button x:Name="btnPrintOnly" Content="Print Only"/>
                                <Button x:Name="btnCutOnly" Content="Cut Only"/>
                                <Button x:Name="btnKickDrawer" Content="Kick Cash Drawer (Till)"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>

                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
